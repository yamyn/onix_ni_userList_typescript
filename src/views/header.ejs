<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand text-primary" href="/v1/users/">UsersList</a>
    <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
    >
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a class="nav-link 
            <% if (template === 'users/table.ejs') { %> active <% } %>
            " href="/v1/users/">Table</a>
        </li>
        <li class="nav-item
        <% if (template === 'users/statistic.ejs') { %> active <% } %>
        ">
            <a class="nav-link" href="/v1/users/statistic/">Statistic</a>
        </li>
        <li class="nav-item">
            <a class="nav-link
            <% if (template === 'emails/table.ejs') { %> active <% } %>
            " href="/v1/emails/">Emails</a>
        </li>   
        <li class="nav-item">
            <a class="nav-link
            <% if (template === 'screens/table.ejs') { %> active <% } %>
            " href="/v1/screens/">Screens</a>
        </li>
        </ul>
        <div class="profile form-inline my-2 my-lg-0 dropdown">
        <a
            class="nav-link dropdown-toggle text-light"
            href="#"
            id="navbarDropdown"
            role="button"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
        >
            <%=adminName %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="/v1/auth/logout">Logout</a>
        </div>
        </div>
    </div>
    </nav>
</header>